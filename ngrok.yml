version: "2"
# Keep token empty in repository; set your own token locally.
authtoken: YOUR_NGROK_AUTH_TOKEN
tunnels:
  frontend:
    proto: http
    addr: 3000
  backend:
    proto: http
    addr: 8000
